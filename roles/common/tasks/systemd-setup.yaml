
- name: copy the {{ components }} service file
  ansible.builtin.copy:
    src: "{{ components }}.service.j2"
    dest: "/etc/systemd/system/{{ components }}.service"
- name: daemon reload the service
  ansible.builtin.systemd_service:
    daemon_reload:  true